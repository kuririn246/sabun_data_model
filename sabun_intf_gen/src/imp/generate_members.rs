use sabun_maker::intf::member_desc::{MemberDescs, MemberDesc};
use sabun_maker::structs::RustMemberType;

pub fn generate_members(mems : &[MemberDesc]){
    for mem_desc in &mem_descs{
        match mem_desc.member_type(){
            RustMemberType::
        }
    }
}