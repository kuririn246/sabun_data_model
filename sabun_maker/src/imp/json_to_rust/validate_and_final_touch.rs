use std::collections::HashMap;
use crate::rust_struct::{RustValue, RustObject};
use crate::error::Result;

pub fn validate_and_final_touch(map : HashMap<String, RustValue>) -> Result<RustObject>{
    todo!();
}