use crate::structs::rust_list::RustList;
use crate::imp::version_adjuster::adjust_value::adjust_obj;
use crate::error::Result;

pub fn adjust_list(new : RustList, old : RustList, validation : bool) -> Result<RustList>{
    // let new_default = adjust_obj(new.default, old.default,validation)?;
    // for (key, value) in old.list{
    //     construct_new_sabun(&new_default.renamed
    // }
    
todo!()

}